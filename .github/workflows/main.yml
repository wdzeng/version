name: Build

on:
  workflow_dispatch: ~
  push:
    branches:
      - main
    paths:
      - src/index.js

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: wdzeng/action-of-action@v5
